{{- $intTLS    := .Values.internalTLS -}}
{{- $expose    := .Values.expose -}}

{{- if or (eq $intTLS.certSource "certmanager") (and $expose.enabled (eq $expose.tls.certSource "certmanager")) }}
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "in-cloud-console-landing.fullname" . }}-selfsigned
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "in-cloud-console-landing.labels" . | nindent 4 }}
spec:
  selfSigned: {}

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "in-cloud-console-landing.fullname" . }}-ca
  namespace: {{ .Release.Namespace }}
spec:
  commonName: sgroups-ca
  isCA: true
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: {{ include "in-cloud-console-landing.fullname" . }}-selfsigned
  privateKey:
    algorithm: ECDSA
    size: 256
  secretName: {{ include "in-cloud-console-landing.fullname" . }}-ca-secret

---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "in-cloud-console-landing.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  ca:
    secretName: {{ include "in-cloud-console-landing.fullname" . }}-ca-secret
{{- end }}
