apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "in-cloud-console-landing.fullname" . }}-test-connection"
  labels:
    {{- include "in-cloud-console-landing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "in-cloud-console-landing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
