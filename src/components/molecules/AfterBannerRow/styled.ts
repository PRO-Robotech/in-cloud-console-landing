import styled from 'styled-components'

const AfterBannerContainer = styled.div`
  width: 754px;
  margin: 0 auto;
  padding: 20px 30px;
  border-radius: 40px;
  border: 3px solid rgba(255, 255, 255, 1);
  background: rgba(21, 35, 46, 1);

  font-family: 'Raleway', sans-serif;
  font-weight: 700;
  font-size: 20px;
  line-height: 130%;
  letter-spacing: 0%;
  text-align: center;

  span {
    color: rgba(0, 146, 255, 1);
  }
`

export const Styled = {
  AfterBannerContainer,
}
